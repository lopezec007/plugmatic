# Deviations from the implementation spec

| Date (UTC) | Spec ref | Deviation | Rationale | Architectural? |
|---|---|---|---|---|
| 2026-08-02 | Appendix / §6.5 | Programming cable is **CH340 (1A86:7523)**, not FTDI. FTDI kept as secondary candidate ID. | lsusb on the actual cable | no |
| 2026-08-02 | §4.2 | `IRadioCodec.Encode` gained an optional `baseImage` parameter: `Encode(Codeplug ir, ReadOnlyMemory<byte>? baseImage = null)`. Hardware writes encode on top of the same-run `pre-write.bin` so settings/unknown blocks are carried forward; `build` runs encode from scratch. | The DM-32UV image contains opaque per-radio blocks (calibration-adjacent settings) that must survive a write; D7 guarantees a base image is always available on the hardware path. | no (additive) |
| 2026-08-02 | §3.2 | In addition to qdmr, the MIT-licensed community spec github.com/infamy/DM32-Protocol-Spec is used as a primary documentation source (it is the second source for cross-validation; its CPS capture file is vendored as a fixture). | Two-source rule satisfied without hardware risk | no |
| 2026-08-02 | §6.3 (addition) | New invariant (user-directed at ladder step 6): **DMR TX requires a configured operator DMR ID** (`config set dmr.id`). Builder forces all DMR channels RX-only without it; validator rejects TX-enabled DMR channels when `Settings.RadioId == 0`; codec never touches the radio-ID block when the IR carries no ID. | Radio transmitted with a zeroed ID during testing — unidentified DMR TX must be impossible | no (additive safety) |
| 2026-08-02 | §7.5 / ladder | Ladder steps 5–6 completed; per user request the generated Fort Collins plug REMAINS on the radio (original archived as the `factory-golden` read run and in every write run's `pre-write.bin`). | User preference | no |
| 2026-08-10 | §6.4 / format §1 | **Image window widened to the full 0x100000 virtual block space.** The old [0x03000,0x68000) window silently skipped 11 of this radio's 107 mapped blocks, so reads — including `pre-write.bin` backups — were incomplete and those blocks were never written. Protocol now refuses a read whose mapped blocks fall outside the window; legacy-sized images are still accepted (0xFF-padded) so old backups stay restorable. | A backup that skips blocks is not a backup | no (strictly safer) |
| 2026-08-10 | §3.2 / format §4 | **qdmr fact corrected by hardware:** the channel scan-list field is a *literal* 1-based index in bits 6–0 of 0x19, not a nibble at bits 5–2. The shifted encoding made the radio use list index×4. Proven by having the radio itself rewrite three channels (0x01, 0x01, 0x89). The user's pre-plugmatic codeplug carries the same ×4 pattern, so whatever wrote it shared the bug — that image is not evidence. | Two-source rule: hardware outranks both references | no |
| 2026-08-10 | §6.3 (addition) | Radio-wide call-match flags (settings block 0x04, +0x060, bits 0–1) are now authored by plugmatic, written **off** (the radio's shipped default) at the user's request. Read-modify-write of two bits only; the block is never fabricated when absent. | User request; previously pure passthrough | no |
| 2026-08-10 | Spec §1/§10 (scope) | **Second radio added: AnyTone AT-D878UVII+** (`--radio d878uv`), at the user's direction. The v3 spec scopes v1 to the DM-32UV and lists other radios as out of scope, so this is v2 work. Introduced `IRadioDefinition` + `RadioRegistry`; every command is now radio-agnostic. Same disciplines apply unchanged: facts→docs→code, two-source cross-validation, read-only probing first, ladder before writes. | User request | yes — approved by the user |
| 2026-08-10 | d878uv | AnyTone support is **read/backup only** (`SupportsWrite = false`): identify, full read, decode of channels/zones/contacts/radio-ID. `write` refuses for this radio until the format doc is complete and hardware-verified. | Nothing gets written from unverified facts | no |
| 2026-08-10 | §6.3.1 / §5.1 | `RawRecord`/`RawBlocks` are now `[YamlIgnore]`. They are binary fidelity state, not part of the human-facing IR, and were making `read.yaml` mostly byte arrays (1536 of 2741 lines on the D878). Consequence: `write --plug read.yaml` rebuilds records from modeled fields instead of preserving unmodeled bits — byte-exact restore is `write --image <run>/pre-write.bin`, which is the documented recovery path anyway. | read.yaml is specified as the reviewable decoded IR | no |
| 2026-08-10 | d878uv / ladder step 4 | **AnyTone write support blocked by hardware behaviour.** Correctly framed 16-byte writes are ACKed and silently not applied (proven on both unallocated and allocated records, across session close/reopen). Both references write exactly this frame with no prepare/commit step, so either the V101 firmware needs something neither implements, or their AnyTone write paths are stale. `SupportsWrite` stays false; unblocking needs a USB capture of the official CPS writing. | Nothing is written from a protocol we cannot demonstrate works | no |
| 2026-08-10 | §3.5 step 4 (ladder) | **A no-op writeback is not sufficient evidence that a write path works.** Writing identical bytes back passes trivially when the radio ignores writes entirely. Ladder step 4 now requires a *mutation* test: change a byte, read it back, then restore. Added as `plugmatic dev writetest --prove`. | The identical-write test returned a false PASS on the AnyTone | no (strictly stronger) |
| 2026-08-20 | d878uv / ladder step 4 | **The 2026-08-10 "writes are ACKed and not applied" finding was wrong.** The CPS capture showed identical framing to ours, and hardware then showed why our probe failed: writes are *staged* and commit on `END`, and **any read after a write discards everything staged**. Our probe read its target back in the same session, which both returned pre-write bytes and destroyed the write it was checking. Its one cross-session test used addresses (`0x00841200`, `0x00FC07C0`) that are not backed by storage, so the only experiment that could have caught this landed on the only class of address that cannot answer. | Hardware outranks our own earlier conclusions too | no (correction) |
| 2026-08-20 | §3.5 step 4 (ladder) | **A mutation test must read back in a *separate session* on this radio**, or it reports a false negative exactly as loudly as a no-op writeback reports a false positive. `dev writetest` rewritten accordingly. | The 2026-08-10 lesson was right in kind and wrong in detail | no (strictly stronger) |
| 2026-08-20 | d878uv / format §2 | **A write erases the aligned 0x40000 (256 KB) block around it** and keeps only what the session staged. The region table describes 1.63 MB scattered across blocks holding far more (the block at `0x00800000` is covered for 0x2000 of 0x40000, while the CPS keeps channel data at `0x00802000-0x00804000`), so any write would wipe codeplug bytes we have never read. `SupportsWrite` stays **false**; `BuildWritePlan` refuses under-covered blocks by address instead of writing them. Unblocking = read whole erase blocks raw and rewrite them complete (preferred), or extend the region table. | A write path that destroys unmodelled data is worse than no write path | no |
| 2026-08-20 | §3.5 step 4 (ladder) | **New ladder rule: establish the erase granularity before the first write, and check for damage across that whole granularity — not across the bytes written.** Learned by destroying channel banks 0 and 1 with two 16-byte probes that both *passed*, because each only re-read its own 16 bytes. Blast radius was 16,384× the verified region. Data recovered byte-exactly from the CPS capture. `dev writetest` now works only in a block it has confirmed holds no codeplug data. | A probe that cannot see its own crater is not a safety test | no (strictly stronger) |
| 2026-08-20 | d878uv / format §2 | Firmware keeps a 16-byte signature (`FF FF FF FF 22 33 44 55 FF FF FF FF 55 55 AA AA`) at the end of every 0x20000 half-block. It is firmware-managed — present in never-written blocks and restored after an erase — so emptiness checks must ignore it (`Layout.IsFlashMarker`). | Otherwise every block looks "in use" | no |
| 2026-08-20 | §4.2 | `IRadioProtocol.WriteImageAsync` gained a `baseline` parameter (the image just read from the radio) and `IRadioDefinition` gained `SeparateReadWriteSessions`. The AnyTone needs read, write and verify in three distinct sessions; the DM-32UV path is unchanged (`false`, default interface implementation). | The write flow cannot verify in the session that wrote | no (additive) |
| 2026-08-21 | d878uv / §7.5 | **Block-rewrite write path implemented, hardware-tested, and withdrawn.** Reading each erase window in full, splicing the modelled bytes over it and rewriting it complete does preserve unmodelled bytes — verified, 1 byte changed across a 256 KB window with the non-FF count identical. But the same write copied channel bank 0 over channel bank 1, with the transfer log showing every one of its 1,282 frames inside `0x00800000-0x0083FFF0` and none addressed to `0x00840000`. `SupportsWrite` back to **false**; `WriteImageAsync` refuses. | A write path that corrupts a bank it never addressed is not shippable | no (withdrawal) |
| 2026-08-21 | d878uv / format §2 | **Some 0x20000 spans read back duplicated, others do not.** `0x00800000`/`0x00820000` are byte-identical, as are the halves of bank 1, `zoneChannels`, `settings`, `zoneNames`; `contacts[0]` and `0x024C0000` are distinct. This pre-dates any write — it was missed because the §5.4 model was only checked against CPS-*written* addresses, never the gaps. Consequence: the project cannot currently say which addresses are independent storage, which is what blocks writing. | Facts before code | no |
| 2026-08-21 | §3.5 step 4 (ladder) | **Two more ladder rules.** (2) Check the *neighbours* of a write, not just its erase window — the withdrawn path passed a full 256 KB before/after rawdump and had still corrupted the bank next door; only a full-image compare caught it. (3) Where the vendor tool has been captured, treat the captured address set as the writable address set — every byte lost on this radio was at an address the CPS never writes. | Both learned by losing data | no (strictly stronger) |
| 2026-08-21 | d878uv | Added `plugmatic dev rawdump`: reads whole erase windows verbatim (optionally a chosen subset) so before/after damage checks happen at the granularity a write actually affects, rather than through the region table. | The region table cannot see a crater outside itself | no (additive) |
| 2026-08-21 | d878uv / format §2 | **Bank storage model, and write support enabled.** A bank is a 0x40000 window holding 0x20000 of real storage; the upper half is a firmware-made duplicate, not storage. Evidence: the window repeats with period exactly 0x20000 and no smaller, and the CPS writes 0 of its 96,352 bytes at `bank + 0x20000` or above across all 21 banks it touches. The write path now reads each changed bank's storage half, splices the modelled bytes over it and rewrites the unit — never the upper half, never the firmware signature. `SupportsWrite` back to **true**. | The mirrored half was the whole cause of the bank-0-over-bank-1 corruption | no |
| 2026-08-21 | d878uv / §3.5 ladder | Ladder step 4/5 passed on hardware with the corrected model: one-byte channel rename, 640 frames (the same count the CPS sends for that bank, all inside its footprint), intended byte applied, full-image post-write verification matched, **neighbour bank 0 bytes differ**, and restore to the original image byte-identical. | Rules 1-3 all exercised | no |
| 2026-08-21 | d878uv / format §2 | **Open question, flagged not resolved:** after a Plugmatic write the written bank's upper-half duplicate reads erased (bank 0: 20,264 non-FF -> 10,132) and does not regenerate on its own. The CPS never writes those addresses either, so the firmware creates it; we cannot recreate it, because writing that half is what corrupted a neighbouring bank. A Plugmatic-written radio may therefore have one fewer firmware recovery copy than a CPS-written one until the firmware rebuilds it — if it does. | Better a documented unknown than an undocumented one | no |
| 2026-08-21 | §4.2 | `IRadioDefinition.ReEnumeratesAfterSession` added; when set, `RadioSession.OpenAsync` waits for the device node to come back instead of reporting "no serial ports found". The AnyTone drops its USB device after every session, so a command starting right after another one would spuriously fail. | Affected read, write and every dev command | no (additive) |
| 2026-08-21 | d878uv / format §2 | **Open question from earlier today closed:** the upper-half duplicate *does* regenerate — after a power cycle the radio came up normally and bank 0's duplicate was back, matching the restored lower half. Also learned that `settings` lost its duplicate in a bank Plugmatic has never written, so these halves are firmware-managed state that moves in both directions on the radio's own schedule. Nothing should write, preserve or verify them. | The risk logged in §5.8 was real to flag and is now measured | no |
| 2026-08-21 | d878uv / format §2 | **The settings bank drifts on its own.** A read after a power cycle differs from the image written minutes earlier by 647 bytes: one current-zone runtime byte (`0x0250011A`, `00`→`0E`) and 646 bytes of region-tail padding going `00`→`FF` in `dmrAprsMessage` and `settingsExtension` — the firmware rewrote that unit and did not program its padding, exactly as our write path skips all-FF chunks. Host-caused changes ruled out from the transfer log (640 frames, all in `0x00800000`). | Documented, not masked | no |
| 2026-08-21 | §4.2 / d878uv | **VERIFY carried forward:** `D878uvCodec.Compare` is a plain byte comparison; this radio has no volatile-region list. Post-write verification is unaffected (it runs before the radio touches anything), but a backup compared against a later read shows the §5.8.1 drift. Declaring regions volatile stops verifying them, and one power cycle is a single sample, so the list is deliberately not being invented yet. | Losing coverage needs better evidence than this | no |
| 2026-08-21 | d878uv / format §4 | **Slot-assignment order bug, caught by the I3 gate.** `AssignSlots` reuses the base image's already-allocated slots first and then fills the gaps, yielding a set like `[0..51, 100..131, 52..99, …]`, while `Decode` enumerates bitmaps in index order. Any item past the base allocation's first gap was encoded into one slot and read back from another — item 53 onward, for a radio whose bitmap had 52 contiguous channels. Fixed by sorting the chosen slots; the selection is unchanged, only the IR→slot mapping order. Regression tests for channels and contacts fail without the fix. | Never fired before because every prior D878 write was a byte-identical restore, and the radio's own plug was smaller than the first gap | no |
| 2026-08-21 | §7.5 / ladder | **D878 ladder steps 5–6 completed; the generated Fort Collins plug REMAINS on the radio** at the user's request, mirroring the DM-32UV. Built from `--location 80525 --profile colorado-default`: 327 channels (298 DMR, 29 FM), 13 zones, 47 contacts, 13 scan lists. The write read 11 banks, sent 3,291 frames, and the post-write full-image read is byte-identical to `generated.bin`. Transfer log audited: no frame at `bank + 0x20000` or above, none on a flash signature. The previous codeplug is archived as `pre-write.bin` in the write run. | User request | no |
| 2026-08-21 | d878uv / build | RepeaterBook returns HTTP 401 without a token, so the generated plug carries **DMR (RadioID) data only** — no analog ham repeaters. Same limitation the DM-32UV plug was built under. `plugmatic config set repeaterbook.token <token>` and rebuild to add them. | Documented, not a defect | no |
| 2026-08-21 | d878uv / §6.3.3 (D8) | **GMRS TX enabled and the plug rewritten**, at the user's explicit instruction. The D8 acknowledgment was entered on their behalf after surfacing its three clauses — licence held, sole responsibility, and that this radio is not FCC-certified for GMRS so transmitting on it may itself breach Part 95E equipment rules — recorded `2026-08-21T23:27:47Z`. Rebuild changed exactly 15 channels: GMRS 1-7 and 15-22 (462 MHz) `Inhibited` -> `Allowed`. GMRS 8-14 (467 MHz interstitials) and NOAA WX 1-7 remain receive-only, as the acknowledgment states they must. Write touched 1 bank, 284 frames; the radio's image reads back identical to `generated.bin`. | User request; attestation is the operator's, so its substance was shown before being recorded | no |
| 2026-08-21 | d878uv / §6.3.2 | Operator callsign set (`plugmatic config set dmr.callsign KF0UAA`) and the plug rewritten, so the radio identifies as the callsign rather than falling back to the DMR ID. Rebuild diff was a single line; the differential write narrowed to 4 frames (64 bytes) in the settings bank and the radio reads back identical to `generated.bin`. RepeaterBook access is still pending, so the plug remains RadioID DMR data only. | User request | no |
| 2026-08-21 | §6.2 / providers | **RepeaterBook auth corrected and diagnosed.** Their export API closed to unapproved clients on 2026-03-03 and moved to token auth on 2026-03-31; a live probe returns `{"ok":false,"error_code":"auth_missing"}` for `Authorization: Bearer`, `?key=` and `X-API-Key` alike, so our `Bearer` header would have failed even with a valid token. Per their wiki, the preferred header is **`X-RB-App-Token`**, and a *distributed* client-side tool like plugmatic (their examples: QDMR, CHIRP) cannot hold a shared `app_` token — the application must be approved, after which each user mints an app-bound `rbuapp_…` token at `/user/api_apps.php`. A premium subscription alone does not grant API access. Switched to `X-RB-App-Token` and made the 401 explain all of this. | Their stated terms also forbid bypassing technical controls, so the existing "never scrapes HTML" rule stands | no |

